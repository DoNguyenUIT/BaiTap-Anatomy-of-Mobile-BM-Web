<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicAuth"
        tools:targetApi="31">

        <!--
            Khai báo LoginActivity là màn hình khởi chạy của ứng dụng.
            - `android:name=".LoginActivity"`: Chỉ định lớp Activity. Dấu "." ở đầu là viết tắt cho package của ứng dụng (com.example.basicauth).
            - `android:exported="true"`: Cho phép Activity này được khởi chạy bởi các thành phần bên ngoài ứng dụng (ví dụ: launcher của hệ thống). Điều này là bắt buộc đối với Activity chính.
            - `intent-filter`: Khai báo rằng Activity này là điểm vào chính (`MAIN`) và sẽ xuất hiện trong danh sách ứng dụng (`LAUNCHER`).
        -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />

    </application>

</manifest>
